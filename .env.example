# NEXT_PUBLIC_* values are compiled into the browser bundle during `next build`.
# Supply these before the build (or as Docker build arguments); changing only a
# Cloud Run runtime environment variable does not update an existing image.
NEXT_PUBLIC_API_BASE_URL=http://localhost:8088
NEXT_PUBLIC_APP_PROFILE=local
